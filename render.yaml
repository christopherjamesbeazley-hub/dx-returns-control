services:
  - type: web
    name: dx-returns-control
    env: node
    plan: free
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: HOST
        value: 0.0.0.0
      - key: NODE_VERSION
        value: 22
